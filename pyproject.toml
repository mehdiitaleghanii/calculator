[project]
name = "cal"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.104.0",
    "mypy>=1.18.2",
    "ruff>=0.14.6",
    "uvicorn[standard]>=0.24.0",
]
